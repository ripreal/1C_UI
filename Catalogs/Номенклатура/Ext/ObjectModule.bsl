
Процедура ПриЗаписи(Отказ)
	
	Если ЗагружаетсяИзИнтернета Тогда
		
		РегНабор = РегистрыСведений.КурсыТоварныхРынков.СоздатьНаборЗаписей();
		РегНабор.Отбор.Номенклатура.Значение = Ссылка;
		РегНабор.Отбор.Валюта.Значение = Валюта;
		
		ЗаписьНабора = РегНабор.Добавить();
		ЗаписьНабора.Номенклатура = Ссылка;
		ЗаписьНабора.Валюта = Валюта;
		ЗаписьНабора.Период = "20010101";
		ЗаписьНабора.Курс = 1;
		РегНабор.Записать();
		
	КонецЕсли;
	
КонецПроцедуры
